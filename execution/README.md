# Execution (Layer 3)

Place your deterministic Python scripts here.

- Handle API calls, data processing, file operations, database interactions, etc.
- Keep them reliable, testable, fast, and well-commented.
- Load configuration and API credentials from the `.env` file at the root level.
